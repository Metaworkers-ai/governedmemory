# API server dependencies — required to run the FastAPI REST server (E7)
# Install: pip install -r requirements-api.txt
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0
openai>=1.0.0    # only used if OPENAI_API_KEY is set — see api/main.py:_build_embedder
