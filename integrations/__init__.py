"""Third-party benchmark/framework integrations, kept out of core/ on purpose.

Per docs/traction-roadmap.md (Workstream A): these are integrations, not
engine changes -- they depend on core/ (MemoryStore, check_privilege, the
model types), never the other way around.
"""
